
@{
    ViewData["Title"] = "Index";

}

@*<h2>Index</h2>

    <p>Hello from our View Template!</p>*@
<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Creative Button Styles </title>
    @*<meta name="description" content="Creative Button Styles  - Modern and subtle styles &amp; effects for buttons" />
        <meta name="keywords" content="button styles, css3, modern, flat button, subtle, effects, hover, web design" />
        <meta name="author" content="Codrops" />*@
    <link rel="shortcut icon" href="../favicon.ico">
    <link rel="stylesheet" type="text/css" href="css/default.css" />
    <link rel="stylesheet" type="text/css" href="css/component.css" />
    @*<script src="js/modernizr.custom.js"></script>*@
</head>
<body runat="server">
    <div class="container">
       
        <section class="color-8">
         
            <br />
            <p class="perspective">
                <button class="btn btn-8 btn-8g" type="button" id="btnApprove">Approve</button>
            </p>

            <p class="perspective">
                <button class="btn btn-8 btn-8g" type="button" id="btnApprove">Approve</button>
                @*@using (Html.BeginForm("Approved", "Home", FormMethod.Post))
        {*@
                @*<input class="btn btn-8 btn-8g" type="button" name="submit" id="submitButton" value="submit" />*@
               
                
                @*}*@
            </p>

            <p class="perspective">
                <button class="btn btn-8 btn-8g">3D Button</button>
            </p>


        </section>
    </div><!-- /container -->
    
   


    @*<script>
        function ApproveClick() {
            $.ajax({
                type: "POST",
                url: "/Home/Approved",
                async: true,
                success: function (msg) {
                    ServiceSucceeded(msg);
                },
                error: function () {
                    return "error";
                }
            });

        }

    </script>*@
    <script src="js/classie.js"></script>
    <script>
        //classie.remove(this, 'btn-success3d')
        //classie.add(this, 'btn-success3d');


        var buttons7Click = Array.prototype.slice.call(document.querySelectorAll('#btn-click button')),
            buttons9Click = Array.prototype.slice.call(document.querySelectorAll('button.btn-8g')),
            buttons10Click = Array.prototype.slice.call(document.querySelectorAll('input.btn-8g')),
            totalButtons7Click = buttons7Click.length,
            totalButtons9Click = buttons9Click.length;

        buttons7Click.forEach(function (el, i) { el.addEventListener('click', activate, false); });
        buttons9Click.forEach(function (el, i) { el.addEventListener('click', activate, false); });
        buttons10Click.forEach(function (el, i) { el.addEventListener('click', activate, false); });

        function activate() {
            var self = this, activatedClass = 'btn-activated';
            console.log(self);
            console.log(activatedClass);
            if (classie.has(this, 'btn-7h')) {
                // if it is the first of the two btn-7h then activatedClass = 'btn-error';
                // if it is the second then activatedClass = 'btn-success'
                activatedClass = buttons7Click.indexOf(this) === totalButtons7Click - 2 ? 'btn-error' : 'btn-success';
            }
            else if (classie.has(this, 'btn-8g')) {
                // if it is the first of the two btn-8g then activatedClass = 'btn-success3d';
                // if it is the second then activatedClass = 'btn-error3d'

                activatedClass = 'btn-success3d';
               


                // activatedClass = buttons9Click.indexOf(this) === if;
            }
            console.log(this);
            console.log(activatedClass);
            //activatedClass = 'btn-success3d';
            //classie.add(this, 'btn-success3d');
            if (!classie.has(this, activatedClass)) {
                classie.add(this, activatedClass);
                setTimeout(function ()
                {
                    classie.remove(self, activatedClass);
                    console.log(self);
                    console.log(activatedClass);
                }, 2000);
               
            }
            
        }

        //document.querySelector('.btn-7i').addEventListener('click', function () {
        //    classie.add(document.querySelector('#trash-effect'), 'trash-effect-active');
        //}, false);
    </script>
</body>
</html>



