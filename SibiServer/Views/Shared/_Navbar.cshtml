@*<!DOCTYPE html>

    <html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>@ViewBag.Title</title>
    </head>
    <body>
        <div>
            @RenderBody()
        </div>
    </body>
    </html>*@
@model SibiServer.Models.RequestApproval
<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            margin: 0;
        }

        .topnav {
            overflow: hidden;
            background-color: #333;
        }

            .topnav a {
                float: left;
                color: #f2f2f2;
                text-align: center;
                padding: 14px 16px;
                text-decoration: none;
                font-size: 17px;
            }

                .topnav a:hover {
                    background-color: #b9cdff;
                    color: black;
                }

                .topnav a.active {
                    background-color: #2e70ff;
                    color: black;
                }
    </style>
</head>
<body>

    <div class="topnav">

        @*<a href="#myapprovals">My Approvals</a>*@

        @Html.ActionLink("My Approvals", "MyApprovals", "Sibi", new { approverId = Model.ApproverID })</a>
        <a href="#myitems">My Items</a>
        <a> @Html.DisplayTextFor(m => m.GUID) </a>

    </div>

    <div style="padding-left:0px">
        @RenderBody()
    </div>

</body>
</html>
